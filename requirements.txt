# TTB COLA Label Specialist POC — pinned dependencies
# Version-of-record: _bmad-output/planning-artifacts/approved-tech-stack.md §9
# Pin policy: minor line (~=major.minor) unless a patch is named.
#
# Story 1.1 ships the skeleton only. Heavy extraction deps (paddleocr,
# opencv-python-headless, langchain, openai/anthropic/google-genai) are added
# by their owning stories in Epic 2 — kept out here so the build stays fast and
# the `docker run --network none` boot is clean.

# Web / application layer
fastapi~=0.136
uvicorn[standard]~=0.49
pydantic~=2.13
jinja2~=3.1
python-multipart~=0.0.32   # POST form parsing for the token gate (Story 1.5)

# Background jobs (in-process sweep; 3.x stable, NOT the 4.0 pre-release)
apscheduler~=3.11

# dev / quality / test
ruff~=0.15
pytest~=9.0
httpx~=0.28        # FastAPI TestClient transport (test-only)
