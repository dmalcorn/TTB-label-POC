# --- AI / factory tooling & reference templates (not part of the deliverable) ---
# Note: _bmad/ is the BMad framework install (manifests, scripts, config) — tooling,
# so ignored. _bmad-output/ (planning artifacts — architecture.md, PRD, brief, UX)
# is intentionally NOT ignored — it is a deliverable.
_bmad/
.claude/
.agents/
targetsetup/

# --- Python ---
.venv/
venv/
env/
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# --- Secrets / local config ---
.env
.env.*
!.env.example
*.local

# --- Large reference PDFs (kept locally, not in the repo) ---
*.pdf

# --- OS / editor cruft ---
.DS_Store
Thumbs.db
*.swp
*~
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
