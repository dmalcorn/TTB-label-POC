# Deferred Work

## Deferred from: code review of 5-4-benchmark-report-screen (2026-06-14)

- **The closing Recommendations callout is hard-coded prose ("PaddleOCR baseline" / "Claude") that can contradict the data-derived best-in-group highlight — a copy-vs-data decision for Diane.** AC5 requires the recommendations be "consistent with the figures." The best-in-group `.best`/"Recommended" pill IS derived (`_best_key` over the real `field_match_rate`), but the three Recommendations bullets (`templates/benchmark.html` `benchmark__reco`) are static guidance copied verbatim from the spine's grounded benchmarking plan (PaddleOCR baseline + Tesseract fallback; a language model for the judgment checks; Government Warning stays deterministic). The spec EXPLICITLY endorses this copy as "the spine's grounded guidance" (Story 5.4 AC5, Dev Notes "Recommendations … the recommendation copy is the spine's grounded guidance"), so reproducing it is correct per the story. The residual risk: if a future seeded corpus has, e.g., Tesseract out-scoring PaddleOCR (or PaddleOCR absent), the highlighted `.best` row and the "PaddleOCR baseline" prose would disagree on the same screen. Today the prose is named acceptable copy and the derived highlight is the live signal; deciding whether the closing recommendation should be **fully derived** from the figures (name the actual best engine/model in the prose) vs **stay the spine's fixed procurement guidance** is a product/copy call for Diane, not an unambiguous code fix. If derived is wanted, thread the best engine/model display-name into the callout text in `routes_benchmark.py` and render it; if fixed, add a one-line note that the recommendation reflects the benchmarking plan, not necessarily this run's top row. [templates/benchmark.html benchmark__reco / app/web/routes_benchmark.py]

## Deferred from: code review of 5-3-speed-and-cost-statistics-with-cost-per-1000 (2026-06-14)

- **`verification_count` counts ALL submissions, not the covered set the cost figures were computed over — a metric-definition decision for Diane.** `_count_verifications` returns `len(repo.list_submissions_for_scoring(conn))` (`app/benchmark/cost.py:_count_verifications`), i.e. every row in `submissions`, with no join to `ocr_results`/`llm_results`. The LLM layer is optional (schema §1.4: extraction rows may be absent; the pipeline skips entirely when `LLM_ENABLED=false`), so a corpus where only a subset of submissions carry `extract_fields` rows reports a `verification_count` larger than the set the per-model token means actually cover. The story Dev Note says "the count of distinct submissions … the stats were computed over," which argues for the covered set; but cost-per-1,000 is a per-call mean ×1000 figure that does NOT divide by `verification_count` (the count is reported alongside, not a denominator), so today it is presentational, not arithmetic — no figure is wrong, only the count's meaning is ambiguous. Mirrors the deliberate 5.2 "metric definition" deferrals. Decide whether the count should be (a) the whole corpus, (b) submissions with any captured extraction, or (c) per-extractor covered counts, and have Story 5.4 label it accordingly. [app/benchmark/cost.py:_count_verifications]
- **`list_llm_cost_rows` does not filter `is_benchmark_only`, unlike the sibling displayed-extraction readers — a benchmark-scope decision for Diane.** The new reader filters `status = 'OK' AND task = 'extract_fields'` only (`app/db/repositories.py:list_llm_cost_rows`), whereas the displayed-extraction readers in the same file add `AND is_benchmark_only = 0`. For Epic-5 procurement cost the broader population is plausibly intended (the benchmark wants the comparison-only models), but a comparison-only run and the displayed run for the SAME `model_id` would be pooled into one latency/token mean with no way to tell them apart. The omission is currently implicit. Confirm the intended population: if benchmark-only rows belong, add a clarifying comment (and have 5.4 label that the cost table spans benchmark + displayed runs); if not, add the `is_benchmark_only = 0` filter to match the siblings. [app/db/repositories.py:list_llm_cost_rows]

## Deferred from: code review of 5-2-accuracy-scoring-against-ground-truth (2026-06-14)

- **Benchmark "match" = production PASS ∪ REVIEW, not PASS alone (text near-miss banding) — a deliberate metric definition to confirm with Diane.** `_score_text` bands a normalized near-miss with `similarity >= REVIEW_FLOOR (0.85)` as **MATCH** (`app/benchmark/scoring.py:_score_text`), whereas the deployed engine `field_match._compare_text` bands the identical pair as **MISMATCH/REVIEW** (a human reviews it; it is NOT an auto-PASS). This is what the story's own Task-3 / AC1 banding specifies ("both present but below the per-field τ ⇒ MISMATCH" ⇒ at/above τ ⇒ MATCH), so the implementation matches the story — but it means the benchmark's field-match RATE is the rate at which extraction is "close enough for a human to confirm," not the rate the app would auto-PASS without review. The module docstring's "agrees with the deployed matcher by construction" is true for the MATCH-by-equality/substring and numeric-tolerance paths but NOT for the near-miss band. Decide which number FR-21 should report (raw extraction fidelity vs production auto-PASS fidelity); if the latter, band `>= REVIEW_FLOOR` as MISMATCH and ensure Story 5.4's report labels the metric. [app/benchmark/scoring.py:_score_text]
- **OCR-confidence floor is intentionally NOT applied in scoring — OCR match rates are confidence-agnostic.** Production `field_match._compare` downgrades an apparent OCR match read from a row with `confidence < OCR_CONFIDENCE_FLOOR (0.50)` to REVIEW (the cross-type-trap valve). Scoring reads `OcrScoringRow.confidence` but never consults it, so a low-confidence apparent match counts as a full MATCH. The story does not require the floor in the benchmark, and measuring raw extraction correctness independent of the trust valve is a defensible benchmark choice — but it makes the OCR engines' benchmarked rate optimistic relative to the deployed auto-PASS rate (compounds with the near-miss item above). If FR-21 should mirror production auto-PASS, thread `confidence` into the OCR scoring path and bucket sub-floor apparent matches as UNVERIFIABLE. [app/benchmark/scoring.py:score_corpus / _ocr_field_value]
- **Short-gold substring MATCH can over-match on a whole-OCR-blob (inherited from the production matcher by design).** `_score_text`'s `gold_norm in ext_norm ⇒ MATCH` treats a clean substring hit on the whole-label OCR blob as a located match; for a very short gold value (a 2–3 char brand fragment) this can coincidentally substring-match unrelated label text and inflate the brand match rate. This is the SAME heuristic the deployed `field_match._compare_text` uses (the story mandates reuse, not a fork — so the benchmark faithfully reproduces production behavior, including this weakness), and it is already tracked as a 3.3 deferral. Tighten with a min-length / word-boundary guard in `field_match` (not a benchmark fork) if real labels surface false matches; the benchmark inherits the fix automatically. [app/engine/checks/field_match.py:_compare_text (benchmark inherits)]

## Deferred from: code review of 5-1-local-only-toggleable-tracing (2026-06-14)

- **Pre-existing mypy errors in untracked `targetsetup/scripts/{cola_capture,cola_backs}.py` (12 errors) — out of story 5.1 scope.** A full `bash scripts/ci.sh` now halts in Phase 3 (mypy, `set -euo pipefail`) on these two files: missing `var-annotated` hints for local `records`/`typecap`/`seen_md5`/`roles_used`, and `BufferedWriter`-vs-`SupportsWrite[str]` `assignment`/`arg-type` mismatches (files opened in binary mode then handed to `json.dump`/`csv.DictWriter`). These files are **untracked** (`git ls-files targetsetup/` is empty) and **unchanged by this story** (`git diff <baseline> -- targetsetup/` is empty); the story's own prior CI run reported mypy clean over "90 source files", i.e. `targetsetup/` was outside the canonical gate then. They are scratch/capture scripts for the COLA target-corpus tooling, not application code. Story 5.1's own changed source (`app/benchmark/tracing.py`, `app/pipeline/llm.py`) passes `mypy --ignore-missing-imports` cleanly, and the full host pytest suite is green (707 passed, 1 skipped). Fix belongs with the `targetsetup/` tooling owner — either annotate the locals + open the output files in text mode, or add `targetsetup/` to the mypy/ruff `extend-exclude` alongside `auto-run/` if that tooling is meant to be outside the gate. [targetsetup/scripts/cola_capture.py, targetsetup/scripts/cola_backs.py]

## Deferred from: code review of 4-8-disposition-action-bar-and-notes (2026-06-14)

- **`correction_due_at` (the 30-day NEEDS_CORRECTION clock) is never set.** The schema column exists and its cross-column CHECK (`correction_due_at IS NULL OR disposition='NEEDS_CORRECTION'`) is honored — the write path simply leaves it NULL on a NEEDS_CORRECTION decision, and `clear_disposition` NULLs it on undo. The story spec (4.8) describes the deadline column but does not specify the clock-start semantics, the duration source (a literal 30 days vs a configurable window), or where the countdown surfaces (it has no UI in any Epic-4 mockup). Setting an arbitrary `decided_at + 30d` now would assert a regulatory deadline the spec did not adjudicate. Confirm with Diane the intended duration + display surface, then set `correction_due_at` in `record_disposition` on the NEEDS_CORRECTION branch (the CHECK already permits it). [app/db/repositories.py:record_disposition / app/web/routes_review.py:record_disposition]
- **No CSRF token on the disposition / undo / progress POSTs (app-wide auth-model decision).** `POST /review/{id}/disposition`, `/undo`, and `/progress` are same-origin and sit behind the `app/main.py` shared token-gate middleware (Story 1.5); a forged disposition would still need the gate token. Real CSRF hardening (per-form token, SameSite enforcement) is an application-wide decision that applies equally to every POST in the app, not a 4.8-local concern — carried forward from the same 4.6 deferral. Defer until the auth model is formalized beyond the shared token gate. [app/web/routes_review.py]
- **`specialist_id` / per-user attribution is not written; every human event is the shared `Label Specialist` actor.** The disposition + undo audit rows record `actor = "Label Specialist"` (matching the queue's OPENED actor) — there is no per-user identity in the POC. The story spec did not list a specialist-id column among the human-decision writes, and `architecture.md` §264 defines the human actor as the single shared specialist role. Revisit when multi-user identity enters scope (it would also touch the OPENED write in `routes_queue.py`). [app/web/routes_review.py:SPECIALIST_ACTOR]
- **`decided_at` is stored as a full ISO-8601 timestamp with timezone (`datetime.now(UTC).isoformat()`), not normalized to a fixed display format.** The column is the as-filed storage convention (an ISO string), which is correct for storage; any human-facing rendering (e.g. a "decided 3 minutes ago" or a localized date on a future decided-submissions view) is a presentation concern with no surface in the 4.8 mockups. Add a presenter format when a screen actually displays the decision time. [app/web/routes_review.py:record_disposition]
- **The confirm-modal "Approve while a REVIEW/FAIL row is still open" soft-gate has no route-level / automated test (client-only coverage).** The server soft-gate (NEEDS_CORRECTION/REJECT require notes ⇒ 400) IS covered; the *Approve-while-open* confirmation is a client affordance in `static/js/disposition.js` (the modal) that never blocks the server (the engine never decides — contract #4), so it is honestly un-testable at the route layer. A future JS/e2e harness (Story 4.11 a11y/interaction pass is the natural home) should assert the modal fires and that confirm submits. [static/js/disposition.js / Story 4.11]

## Deferred from: code review of 4-6-smart-checklist (2026-06-14)

- **The `usercheck` (human-confirmed) row drops the engine verdict word, showing only "you confirmed" (A11Y-vs-fidelity tension).** When a reviewer manually ticks a REVIEW/FAIL row, `_checklist.html` renders the right cluster as `✓ you confirmed` and the underlying engine verdict word (REVIEW/FAIL) is no longer shown on that row. This is **mockup-faithful** (`mockups/review-workspace.html` shows the confirmed row as the navy ✓ + "you confirmed" with no verdict word) and is defensible — the acknowledgement is what changed, the engine verdict is unchanged and still encoded in `data-verdict` + the row's `cli__right--<verdict>` tint — but a screen-reader user who tabs to a confirmed row hears "you confirmed" without the verdict register it acknowledged. The chip word IS still present for every NON-confirmed state (open/openfail/done), so the verdict register is never hidden for a row that still needs eyes; only the already-acknowledged row elides it. Revisit if an SR-pass audit wants the confirmed row to read e.g. "you confirmed — was FAIL". [templates/_checklist.html usercheck branch]
- **Optimistic-tick UI can transiently disagree with the server under concurrent edits / a slow network (no per-row request serialization).** `static/js/review.js:toggleRow` swaps the row state + counter immediately and reverts only on a failed POST; it does not serialize rapid double-clicks on the same row or guard against two tabs ticking the same submission. The server upsert is itself correct + idempotent (read-modify-write of the single `review_progress` row) and **any** fresh GET fully rehydrates the truth (AR-14), so the only window is a transient client-side display drift that self-heals on reload. Tightening this (disable the row while its POST is in flight, or reconcile from a 2xx body) is UX polish for a single-specialist POC where concurrent same-submission editing is not a real workflow. [static/js/review.js:toggleRow]
- **The `POST /review/{id}/progress` tick write has no CSRF token.** The endpoint is same-origin and sits behind the `app/main.py` token-gate middleware (Story 1.5), and the write is a low-stakes acknowledgement (never a disposition, never the engine verdict — contract #4), so the blast radius of a forged tick is a cosmetic counter nudge that the next GET rehydrates. CSRF hardening is an app-wide auth-model decision (it would apply equally to the Story 4.8 disposition POST), not a 4.6-local concern; defer to whenever the auth model is formalized beyond the shared token gate. [app/web/routes_review.py:set_progress]
- **The `group-conditional` scroll anchor is a standalone `aria-hidden` `<span>` with no focusable section, so a checklist jump to it moves the viewport but not focus.** The AC4 focus fix made the three real group `<section>`s (`group-identity`, `group-gov-warning`, `group-decide`) focusable and `review.js:focusTarget` walks up to the nearest focusable, non-hidden ancestor — which covers `group-mandatory-text` (it lives inside `group-identity`). `group-conditional`, however, is a bare marker emitted only when a conditional check exists and has no enclosing section to focus, so for that one anchor the scroll lands but keyboard focus stays put (no broken/hidden-element focus — the WCAG-safe degradation). When Story 4.7/4.8 gives the conditional bucket a real rendered section, make it `tabindex="-1"` so its jump also lands focus. [templates/review.html:group-conditional]

## Deferred from: code review of 4-5-government-warning-comparison-card (2026-06-14)

- **The reworded gov-warning screen-reader text-equivalent repeats the full ~250-char §16.21 warning twice without pinpointing the char delta (Edge CR finding).** `_gw_diff_text_equivalent` (`app/web/review_view.py`) emits an `usa-sr-only` line of the shape `Required: '<full warning>' — on label: '<full warning>'`; a screen reader therefore reads the entire statute twice with no indication of WHICH characters differ, whereas the visual char-diff highlights exactly the changed run. The A11Y floor **is** met — the discrepancy is named in plain text and survives forced-colors mode — and this mirrors the already-shipped 4.4 `_diff_text_equivalent` shape for short field values, so it is deferred as a usability enhancement rather than a defect. The §16.21 body is long enough that the "read the whole thing twice" cost is real for the screen-reader path; tightening it would mean summarizing the diff opcodes into a concise spoken delta (e.g. "changed: '…' to '…'") instead of echoing both full strings. Revisit when an SR-pass audit of the long-form diffs is scheduled. [app/web/review_view.py:_gw_diff_text_equivalent]

## Deferred from: code review of 4-2-next-submission-by-beverage-type (2026-06-14)

- **`.empty-note` font-size 15px is below the project-context body-text floor (≥16px).** `static/css/brand.css:417` sets `.empty-note { font-size: 15px; }`, while `project-context.md` Accessibility floor says "body ≥16px". This value is **explicitly mandated verbatim by the story spec** (Story 4.2 Task 5: "`font-size:15px`", mirroring `mockups/queue.html`'s `.empty-note`), and the sibling 4.1 empty-state `.helper` uses 16px. This is a genuine spec-vs-accessibility-floor tension that the story author already adjudicated in favor of the mockup value, so it is deferred rather than overridden in review — bumping it to 16px (or a token) is a one-line edit if the accessibility floor is ruled to win for this secondary/informational line. Confirm the intended value with Diane (mockup-exact 15px vs floor-compliant 16px). [static/css/brand.css:417 `.empty-note`]

## Deferred from: code review of 3-6-hybrid-class-type-designation-check (2026-06-14)

- **Residual incidental-mention vs genuine cross-label conflict is not deterministically separable (CR F2).** The conflict detector is now ANCHORED to the declared `class_type_designation` and the `liqueur`/`cordial` group is excluded from `CONFLICT_GROUPS` (CR F1, patched) — so the un-anchored "any two stray OCR words ⇒ FAIL" behavior and the compound-liqueur false-FAIL are gone. What remains: when the product DECLARES a recognized base class (e.g. bourbon) AND a *different* mutually-exclusive group keyword appears anywhere in the joined back-label OCR (a cocktail recipe "splash of vodka", a pairing note), the check still emits a deterministic FAIL — indistinguishable, by keyword presence alone, from the genuine AC3 cross-label contradiction (bourbon front / vodka back) that AC3 REQUIRES to FAIL. Deterministically separating "incidental mention" from "second designation" needs positional/same-field-of-vision analysis (Story 3.7 `positional`) or a semantic read (out of POC scope). Tightening this to require the conflicting keyword to be a *designation-shaped* token (adjacent to a class word, in a label's primary field) is the fix when the positional check lands; until then the conservative AC3-honoring behavior stands. [app/engine/checks/class_type.py:_detect_conflict]
- **OCR-fallback recognition keys on incidental marketing/recipe words → possible false PASS (CR F6).** When the application `class_type_designation` is empty/unrecognized, recognition falls back to scanning the entire joined OCR blob for any catalog keyword (`_recognized_keyword(ocr_text)`). A back-label recipe or marketing line ("perfect in a gin & tonic", "Rum Runner brand") contains a keyword and yields a confident PASS for a product whose actual designation may be novel/absent. Bounded (PASS only, and only when the app value is unrecognized; the deterministic FAIL paths are unaffected) and the seeded corpus does not exercise it. Add a front-label / primary-field restriction to the OCR fallback when the positional check (3.7) provides field-of-vision data. [app/engine/checks/class_type.py:_recognized_keyword fallback at class_type()]
- **Catalog has no bare `rye`/`corn` keyword (CR F4) — a label designated simply "Rye" or "Pennsylvania Rye" routes to REVIEW, not PASS.** The catalog carries only the two-word `rye whisky`/`rye whiskey` and `corn whisky`/`corn whiskey`, though the story (Task 1) names "rye" as a recognized base class. Outcome is fail-safe (REVIEW → defer to human, never a wrong FAIL), and the catalog is deliberately DATA/tunable (a one-line edit in `rulesets/class_type.py`). Confirm with Diane whether bare "Rye"/"Corn" should PASS deterministically; if so add the bare keywords to `KEYWORD_GROUPS` (group `whisky`). Left as a tuning decision rather than silently widening recognition. [app/engine/rulesets/class_type.py:KEYWORD_GROUPS]
- **App↔label class-VALUE mismatch is not detected here by design (CR F5).** A single-label submission whose app says `Vodka` but whose OCR reads only `GIN` is not a cross-label conflict (one group in OCR) and PASSes the recognition path. The application-value vs OCR-value match of `class_type_designation` is Story 3.3 `field_match`'s concern (the story scope boundary explicitly carves this out); this HYBRID check answers class/type VALIDITY + cross-label conflict, not the field-value match. Flagged so the gap is explicit, not implicit. [app/engine/checks/class_type.py]

## Deferred from: code review of 3-5-per-type-deterministic-format-checks (2026-06-13)

- **OCR-noise hardening beyond negative/zero sizes (F7).** The net-contents scan now drops `ml <= 0` (negative/zero → `None` → REVIEW, never a spurious FAIL) and prefers a `net`-cued token over a stray serving-size token. Three residual OCR-artifact classes still degrade to an opaque path rather than a parsed size: (1) **non-metric units** (`cl`, `fl oz`, `pint`) — unknown to `_TOKEN_RE`, so they are never seen as a size token and the check REVIEWs (carried as a `normalize` deferral since 3.1; the metric standards-of-fill corpus does not exercise them); (2) **thousands-separated numerics** (`1,000 mL`) — the `(?<![\d.,])` lookbehind splits on the comma so only `000 mL` is captured (→ not an approved size → REVIEW, fail-safe); (3) **implausibly large OCR garbage** (`75000 mL`) — captured cleanly but simply absent from `STANDARDS_OF_FILL` → REVIEW. All three already fail-safe to REVIEW (AC4), so this is robustness polish, not a defect. Add comma-stripping + a `cl`/customary unit branch in `_scan_unit_tokens` if real photo OCR surfaces them. [app/engine/checks/format_checks.py:_scan_unit_tokens / _find_net_contents_ml]
- **`STANDARDS_OF_FILL` carries four sizes beyond the literal §5.203 list without a per-size source note (F9).** The frozenset includes `1800`, `900`, `720`, and `355` mL alongside the documented post-2020 §5.203 metric set. These are real in-market sizes (e.g. 720 mL Japanese spirits, 355 mL single-serve) and the table is deliberately DATA/tunable so a §5.203 amendment is a one-line edit — but a PASS on one of these four would assert "approved standard of fill" beyond what the cited regulation text in `docs/` substantiates. Confirm the intended approved-size set with the regulatory owner (Diane) and either cite the amendment that adds them or trim them; until then a PASS on those four is broader than the cited authority. [app/engine/rulesets/format_checks.py:STANDARDS_OF_FILL]

## Deferred from: code review of 3-4-government-warning-exact-verification (2026-06-13)

- **Three `FormattingRules` flags are declared as DATA but never consulted by the check (AC4-intent gap).** `header_all_caps`, `one_statement`, and `segment_markers` live in `app/engine/rulesets/government_warning.py:FormattingRules` but the check hard-codes the equivalent behavior (header all-caps via a literal `!= data.HEADER`; one-statement/segment-markers implicitly via the exact body prefix-match). Only `mixed_case_capitals` and `bold_required_but_unverifiable` are read. AC4's literal requirement — the §16.21 **text + citation** live as data and are imported — IS satisfied (the guard tests pass; a Part-16 amendment is a one-line data edit). Wiring the three remaining flags into conditional logic would add branches with no behavioral change today (the rules are all currently `True`), so per the project-context "avoid over-engineering / don't design for hypothetical future requirements" rule this is deferred rather than patched. Make the header/segment assertions read their flags if a beverage type ever needs to relax them (none does today). [app/engine/checks/government_warning.py / app/engine/rulesets/government_warning.py]
- **Whitespace-only normalization does NOT fold non-whitespace OCR artifacts (curly quotes, fullwidth/compatibility punctuation, ligatures) → a semantically-identical warning with such an artifact FAILs as reworded.** This is the deliberate `normalize()` exception inverted: the check intentionally avoids NFKC folding + casefold-of-punctuation to preserve the exact-punctuation/casing assertions AC1/AC2 depend on, but that also means a fullwidth comma `，` (U+FF0C) or a curly apostrophe in the §16.21 body is not repaired and the body mismatches. The §16.21 BODY is pure ASCII and the seeded corpus OCR is ASCII, so this is latent today. If real photo OCR surfaces compatibility-variant punctuation, add a TARGETED, whitespace-adjacent compatibility fold (NFKC on punctuation only, never casefold) — not a switch to `app/normalize.py`, which would destroy the casing/trailing-punctuation checks. [app/engine/checks/government_warning.py:_collapse_ws / _body_matches]

## Deferred from: code review of 3-3-field-match-check-with-tolerance-bands (2026-06-13)

- **Substring-MATCH heuristic can over-match on very short text values (F4).** `_compare_text` treats a normalized substring hit on the whole-label OCR blob as a located MATCH (`app_norm in ext_norm ⇒ PASS`). For a very short application value (e.g. a 2–3 char brand fragment) this could MATCH a coincidental substring elsewhere on the label and PASS when it should REVIEW. The story explicitly sanctions the whole-label-blob substring heuristic for the OCR fallback, so this is a judgment-call tightening, not a defect. Add a min-length / word-boundary guard if real labels surface false PASSes. [app/engine/checks/field_match.py:_compare_text]
- **Scratch-only LLM extraction with no persisted `OK` `llm_results` row would drop the value (provenance watch-item).** `_extracted_from_llm` requires a persisted row to attribute `source_llm_result_id` to, so if `ctx.scratch["llm_extraction"]` were ever set WITHOUT a persisted `OK` row, the LLM value is silently dropped and it falls back to OCR. Not reachable today: `engine_stage` runs LAST, after `llm_stage` persists the `OK` row, and the FK requires a real row. Revisit if pipeline stage ordering ever changes so the engine can run before the LLM row is persisted. [app/engine/checks/field_match.py:_extracted_from_llm]

## Deferred from: code review of 3-2-rulesets-as-data-executor-with-verdict-provenance (2026-06-13)

- **`CheckContext.llm_results` is wired empty; the LLM stage's extraction in `StageContext.scratch` is not forwarded.** `run_checks` builds `CheckContext(..., llm_results=[])` and never reads `ctx.scratch`. Correct for 3.2 (the only evaluator is the honest `REVIEW` placeholder; the hybrid class/type check that consumes the model reading is Story 3.6). **Story 3.6** must wire the LLM stage's output (`scratch`/`llm_results`) into `CheckContext` so its evaluator can read the VLM's image reading — and must NOT pass `ctx.ocr_text` into any model call (VLM-only invariant; the seam co-locates both fields but does not structurally prevent the misuse). [app/engine/run_checks.py:run_checks / app/engine/checks/__init__.py:CheckContext]
- **`source_date` is a single `"2022-01-08"` constant across all rows, including the Part 16 Government-Warning row.** The post-2022 Part 5 reorganization date is accurate for the Part 5 citations; Part 16 (§16.21) is not part of that reorg, so one shared source date for the Gov-Warning row is semantically loose. Spec only requires a non-empty ISO date (the test asserts presence), so this is cosmetic/provenance-fidelity only. Revisit if per-citation source dating becomes a procurement/evaluator requirement. [app/engine/rulesets/distilled_spirits.py:_SOURCE_DATE]
- **`net_contents`/`name_address` record only the lead CFR section where the source doc lists several** (`§5.70` vs `§5.70/§5.203`; `§5.66` vs `§5.66/§5.67/§5.68`). This matches the story's Task-1 suggested rows exactly (spec-sanctioned), but the stored citation is narrower than the full set. Expand to the full citation set if/when the standard-of-fill (§5.203) and responsibility-phrase (§5.67–.68) checks land (Story 3.5). [app/engine/rulesets/distilled_spirits.py]

## Deferred from: code review of 3-1-normalization-and-verdict-roll-up-contracts (2026-06-13)

- **`normalize`/`parse_numeric` disagree on a unitless numeric value (`"45"`).** For `alcohol_content`/`net_contents`, `normalize("45", …)` falls back to the text pipeline and returns `"45"` (a plain-text comparison) while `parse_numeric("45", …)` returns `(None, None)` (unparseable, because no unit is adjacent). A label literally filed as `45` with no `%`/unit therefore bypasses the numeric tolerance band in Story 3.3 and compares as opaque text. Settle the single intended behavior (assume a unit, or reject both ways) in **Story 3.3** where the tolerance bands consume `parse_numeric` and the right default is observable. [app/normalize.py:normalize / parse_numeric]
- **Negative magnitudes accepted as a valid canonical value.** `parse_numeric("-5%", "alcohol_content")` → `(Decimal("-5"), "%")`; a physically impossible negative ABV/volume normalizes cleanly with no range guard. Validation/range checking is explicitly Story 3.3 scope (tolerance bands); add the non-negativity guard there, not in the pure contract. [app/normalize.py:_NUMBER]
- **Non-metric / unsupported net-contents units (`pint`, `fl oz`, `gallon`) silently fall back to raw text.** `_UNIT_PATTERNS` knows only `% proof ml l`; a US-customary filing degrades to an opaque text comparison rather than a parsed `(number, unit)`. Acceptable for the metric-standard-of-fill seeded corpus (27 CFR 5.203/4.72); revisit if real imported/customary filings land. [app/normalize.py:_UNIT_PATTERNS]
- **`v_field_comparisons` uses `CREATE VIEW IF NOT EXISTS` — a future change to the view's derivation logic is NOT re-applied on `init_db` against an existing DB.** This matches the project's established idempotence pattern for every schema object and the DDL is verbatim from `database-schema.md` §1.5, so it is correct today; flagged only because a view (unlike a table-add via the column ledger) has no migration path. If the `extracted_source` derivation ever changes, add a `DROP VIEW IF EXISTS` + recreate or a migration step. [app/db/schema.sql:v_field_comparisons]
- **Schema permits a `MATCH`/`MISMATCH` row whose source FK was nulled by `ON DELETE SET NULL`.** Deleting the referenced `ocr_results`/`llm_results` row nulls `source_*_id` (so the view's `extracted_source` → NULL) while `match_status` still reads `MATCH` — an inconsistent state with no CHECK/trigger tying the two. DDL is verbatim per §1.5 (spine wins); Story 3.1 does not write these rows. Revisit a consistency guard when the writers (3.2/3.3) and any delete path exist. [app/db/schema.sql:field_comparisons]

## Deferred from: code review of 2-5-toggleable-llm-extraction-and-ocr-only-fallback (2026-06-13)

- **Build-host SDK/model verification for the VLM path (offline venv can't import the provider SDKs).** Confirm on the build host: (1) OpenAI `max_tokens` is accepted by the chosen model — newer/reasoning models want `max_completion_tokens`; the default `gpt-4o-mini` accepts `max_tokens`, the `local` OpenAI-compatible servers vary; (2) the default model ids resolve (`gpt-4o-mini` / `claude-opus-4-8` / `gemini-2.0-flash`) — they are config-swappable via `LLM_MODEL_ID` and the benchmark sets them explicitly; (3) Google `types.HttpOptions(base_url=...)` + `types.Part.from_bytes(data=, mime_type=)` keyword names against pinned `google-genai~=2.8`. Every mismatch degrades cleanly to an ERROR `llm_results` row (never an abort), so these are correctness-of-real-run checks, not stability risks. [app/adapters/llm/{openai,anthropic,google}.py, app/config.py]

- **AC5 Docker `--network none` seeded-corpus sweep not literally executed** — the offline test suite + egress-origin guard + SDK-free import path prove the gating and boundary, but the literal AC5 Given (`docker run --network none -e LLM_ENABLED=false` sweeping the seeded corpus to `READY_FOR_REVIEW`) is documented and unit-proxied, not demonstrated. Run the real container sweep on the build host (same discipline carried forward from Story 2.4). [README "Offline egress smoke test"]
- **Google adapter passes `http_options` as a plain dict, not `HttpOptions`** — `genai.Client(api_key=..., http_options={"base_url": ...})` only fires on the `LLM_BASE_URL`-set (production/in-firewall endpoint-swap) path, which is the whole point of the firewall story. `google-genai` is not installed in the offline venv so it is unverified locally; the SDK likely accepts an `HttpOptionsDict`, but confirm against pinned `google-genai~=2.8` on the build host — a silent failure here means every Gemini call degrades to an ERROR row in production. [app/adapters/llm/google.py:794]
- **Truncated LLM response (`max_tokens=1024`) stored as `status="OK"`** — adapters read `message.content`/text blocks but never inspect `finish_reason`/`stop_reason`, so a response truncated at the cap is recorded as a clean extraction; a downstream `json.loads` (Epic 3) would raise on the partial JSON. Parsing/validation is Epic 3 scope — revisit there (raise the cap or mark truncation honestly). [app/adapters/llm/openai.py:944 / anthropic.py:707]

## Deferred from: code review of 2-3-local-opencv-image-preprocessing (2026-06-13)

- **Variant filename uniqueness relies on unique source basenames** — variants are named `{Path(src).stem}__enhanced.png`/`__binarized.png`. Safe for the seeded corpus (stems are `<ttb_id>_<NN>_<ROLE>`, globally unique), but two `label_images` sharing a stem (a future upload, or `a.jpg`+`a.png` in one submission) would overwrite each other and both DB rows would point at one file. Add a `label_image.id` qualifier to the variant name when duplicate/untrusted filenames become possible. [app/pipeline/preprocess.py:preprocess_image]
- **Glare detector misses flat (gradient-free) glare** — `normalize_illumination_glare` gates on background luma **spread**, so a uniformly blown-out hotspot with no surrounding gradient is a no-op (the inpaint branch is nested inside the spread gate). Deliberate tradeoff to avoid false-firing on plain white labels (Debug Log). Revisit tuning against real phone photos. [app/pipeline/preprocess.py:normalize_illumination_glare]
- **Orientation not normalized (EXIF + >45° skew)** — `IMREAD_COLOR` ignores EXIF orientation and `_detect_skew_angle` folds any angle into (-45, 45], so a 90°/180° rotated photo is never straightened and a ~45° skew flips correction direction at the fold boundary. The seeded fixtures are upright; this bites real uploaded phone photos. Add EXIF-orientation handling + a >45° branch when real uploads land. [app/pipeline/preprocess.py:decode_normalize_color / _detect_skew_angle]
- **Enhanced variant is grayscale, not color** — AC2/image-handling §3 frame the PaddleOCR-targeted enhanced variant as "grayscale/color"; PaddleOCR/PP-OCRv5 often prefer color. Defensible POC choice (the chain works on intensity); reconsider emitting a color enhanced variant when Story 2.4 wires PaddleOCR and can measure the accuracy delta. [app/pipeline/preprocess.py:preprocess_image]
- **Path robustness for untrusted inputs** — `SOURCE_IMAGES_DIR / image.filename` is not containment-checked (a `../`/absolute `filename` would escape the read-only fixture root), and stored variant paths are basenames relative to `generated_images_dir` with no record of which root they belong to (fragile if the config changes between the preprocess and OCR runs). Both are safe today (trusted seeded basenames + stable config); sanitize the source path and pin the resolution base when real uploads land. [app/pipeline/preprocess.py:preprocess_stage]

## Deferred from: code review of 2-2-background-sweep-and-submission-lifecycle (2026-06-12)

- **No per-job timeout — a hung stage wedges the whole pipeline** — `sweep` does `future.result()` with no timeout, and `job_defaults={'max_instances':1,'coalesce':True}` means while one tick is blocked on a hung worker, no further sweeps fire. Not triggerable by 2.2's passthrough stage, but the seam is explicitly designed for heavy native stages (Tesseract/Paddle/LLM) to plug in with zero scheduler changes — those stages can hang. Add a per-job timeout / hang-containment (and revisit the `shutdown(wait=True)` interaction) in the OCR (2.4) / LLM (2.5) stage stories. [app/pipeline/scheduler.py:sweep]
- **Audit-timeline ordering depends on a test-only tiebreak** — `audit_events.occurred_at` is `CURRENT_TIMESTAMP` (1-second resolution); a fast submission emits all four events in the same wall-clock second, so correct order needs `ORDER BY occurred_at, id`. That `, id` tiebreak currently lives only in the test `_events` helper. No production audit-timeline reader exists yet, but the review-workspace timeline UI story MUST order by `(occurred_at, id)` or events render out of order. [app/db/schema.sql / future timeline reader]
- **`actor` string diverges from the schema-doc convention** — pipeline writes `actor="pipeline"`; `database-schema.md` §1.7 documents per-job actors `system:ocr_job` / `system:analysis_job`. Schema-legal (free-text TEXT, no CHECK) and AC2's "with actor" is satisfied, but audit-trail provenance consistency matters for the procurement/evaluator story. 2.4/2.5 should emit the documented per-job actor names. [app/pipeline/status.py:PIPELINE_ACTOR]
- **`advance` permits multi-step forward jumps** — the guard only requires the target be strictly later in `FORWARD_STATUSES`, so e.g. `PROCESSING→DECIDED` would pass while skipping `READY_FOR_REVIEW`/`IN_REVIEW` and their audit rows. Not exercised in 2.2 (only `→READY_FOR_REVIEW` is called), but future lifecycle stories must not skip intermediate transitions; consider a single-step assertion in `advance`. [app/pipeline/status.py:advance]
- **AC1 atomicity test is serial, not concurrent** — `test_claim_is_atomic_only_one_winner` runs the two `claim_for_processing` calls sequentially on one thread, proving the conditional-`WHERE` guard but not a true concurrent race (the underlying atomic `UPDATE` is correct regardless). Strengthen with a genuine two-thread race on a file-backed WAL DB if a higher-fidelity proof is wanted. [tests/test_pipeline.py:test_claim_is_atomic_only_one_winner]

## Deferred from: code review of 2-1-engine-agnostic-ocr-llm-adapter-contracts (2026-06-12)

- **`word_boxes` JSON serialization is fragile to non-plain types** — `insert_ocr_result` does `json.dumps(result.word_boxes)` with no `default=`/coercion. `word_boxes` is typed `list[dict]`, but a real PaddleOCR engine (Story 2.4) commonly returns numpy scalars in box coords, which raise `TypeError: not JSON serializable` mid-insert. The contract requires plain in-memory types, so the fix belongs in the OCR adapters (Story 2.4): coerce boxes to plain `int`/`float` before constructing `OcrResult`. Add an adapter test with a numpy-bearing box. [app/db/repositories.py / Story 2.4 adapters]
- **`llm_results.label_image_id ON DELETE SET NULL` path untested** — `submission_id` is `ON DELETE CASCADE` and a benchmark-only LLM row already has `label_image_id=NULL`, so the SET-NULL transition only fires when a label image is deleted out from under a non-benchmark LLM row — never exercised. Add a deletion test if/when label-image deletion becomes a real path. [tests/test_adapters.py]
- **Partial-batch durability of the multi-engine write** — `connect()` commits only *after* the `with` body (post-yield), so a pipeline that inserts one engine's OCR row then raises before finishing the unit loses the earlier row on close (all-or-nothing). That atomic behavior is defensible, but whether per-engine partial progress should survive is an Epic-2 pipeline transaction-granularity decision. Settle it in the scheduler/lifecycle story (2.2). [app/db/connection.py]

## Deferred from: code review of 1-6-deploy-to-railway-with-run-from-readme (2026-06-12)

- **Concurrent seed race on the Volume** — `_seed_if_empty`'s `COUNT(*)==0` check and `seed()` run in separate connections (non-atomic check-then-seed), and `seed()` opens a *deferred* `BEGIN` rather than `BEGIN IMMEDIATE`. A Railway rolling-redeploy overlap (old+new container on one `/data/app.db`) or a future second writer (Epic-2 pipeline) could clobber-reload the corpus or hit `SQLITE_BUSY` past the 5s busy_timeout and crash-loop. Reinforces the existing "Concurrent seed/reset vs startup init" item — fold the atomic check (`BEGIN IMMEDIATE` + re-check count inside the txn) into the Epic-6 reset story. [app/db/seed.py / app/main.py]
- **Silent off-Volume seeding if the mount is missing** — `init_db` does `path.parent.mkdir(parents=True, exist_ok=True)`, so a missing/misconfigured Railway Volume mount at `/data` is masked: the app creates `/data/app.db` on the ephemeral container FS and seeds it, looking healthy while losing data on every redeploy (defeats AC-1's "survives redeploys"). A hard mount-assertion would break the legitimate local `data/app.db` path, so this stays an operator-discipline item; the railway-deployment.md playbook documents the `railway volume add` step. Consider a startup log line when `DATABASE_PATH` resolves outside a known mount. [app/db/connection.py:68]

## Deferred from: code review of 1-1-containerized-fastapi-skeleton (2026-06-12)

- **LLM fail-fast validation** — `LLM_ENABLED=true` with absent `LLM_PROVIDER`/`LLM_BASE_URL` yields an inconsistent Settings with no fail-fast. Add a validator in the Epic-2 story that wires the LLM. [app/config.py]
- **`access_token` empty-vs-absent** — `ACCESS_TOKEN=` (empty string) is not distinguished from absent (`None`). Decide semantics in the Story 1.5 auth gate. [app/config.py:41]
- **Import-time settings crash tripwire** — module-level `app = create_app()` runs at import; once any Settings field gains a real validator, `uvicorn app.main:app` boot will crash at import. Factory-path tests import `create_app`, not `app`, so they won't catch it. [app/main.py:38]
- **Settings caching** — `get_settings()` re-parses the environment on every call. Consider `functools.lru_cache` when settings become hot. [app/config.py:50]
- **Unpinned apt packages** — `tesseract-ocr`, `libgl1`, `libglib2.0-0` are unpinned in the Dockerfile, weakening the offline-pinned/reproducible claim. Build-time only, no runtime egress impact. [Dockerfile:16-19]
- **`/healthz` vs future auth gate** — a global auth gate (Story 1.5) added without exempting `/healthz` would 401 the Docker HEALTHCHECK. [Dockerfile:39 / app/main.py]
- **`tests/` in production image** — Dockerfile COPYs `tests/` into the runtime image (bloat/attack surface). Intentional now (in-container `pytest` verifies AC-2), revisit for prod hardening. [Dockerfile:33]

## Deferred from: code review of stories 1.1+1.2+1.3 (2026-06-12)

- **`updated_at` trigger recursion default** — `trg_submissions_set_updated_at` relies on the default `recursive_triggers = OFF`, never asserted in `connection.py`. Add a `WHEN OLD.updated_at = NEW.updated_at` guard or set the PRAGMA explicitly. Documented reliance on a stable SQLite default. [app/db/schema.sql / app/db/connection.py]
- **Concurrent seed/reset vs startup init** — no busy/lock handling for a `seed()`/`POST /reset` racing the startup `init_db` on one file. Use `BEGIN IMMEDIATE` or an app-level lock. Belongs to the Epic-6 reset story. [app/db/seed.py]
- **`audit_events` commit boundary** — table was committed under the Story 1.2 commit (`a92d674`) though it is 1.3's need. Cosmetic commit-boundary smell; no code action. [app/db/schema.sql]
- **Seed lower image-bound** — `_insert_label_images` enforces only `>10`; a 0-image submission would seed silently though AC-4 says 1–10. Add a lower-bound guard when `POST /reset` accepts external CSVs. [app/db/seed.py:74-79]
- **Government Warning canonical text** — `GOV_WARNING`/`GOV_WARNING_REWORDED` are literal constants in the dev-only `fixtures/generate.py` (legitimate as fixture image content). Epic-3's §16.21 wording check MUST read the canonical text from a Ruleset row, never duplicate this constant. [fixtures/generate.py]
- **Four root contract placeholder modules** — `app/contracts.py`/`normalize.py`/`verdict.py`/`disposition.py` named in Story 1.1's architecture tree were never created (only package dirs exist). Create stubs or leave deferred to Epics 2/3. [app/]

## Deferred from: code review of stories 1.4+1.5 (2026-06-12)

- ~~**`Secure` cookie flag behind Railway's proxy (Story 1.6)**~~ — **RESOLVED in Story 1.6 (code review 2026-06-12).** `railway.toml` `startCommand` now runs uvicorn with `--proxy-headers --forwarded-allow-ips=*`, so `X-Forwarded-Proto: https` is trusted, `request.url.scheme` reflects the real https hop, and the access cookie ships `Secure` in production. Route code at `app/web/routes_access.py:55` was already correct; regression `test_start_command_trusts_railway_proxy_headers`.
- **303 gate redirect coerces non-GET → GET** — every non-exempt request (any method) is `RedirectResponse("/access", 303)`, turning a future unauthenticated `POST` into a silent GET of the gate instead of a 401. Latent; only `GET /` exists today. Revisit when a protected non-GET route lands. [app/main.py]
- **Stale cookie not cleared on denial** — `POST /access` denial returns 401 without `delete_cookie(ACCESS_COOKIE)`; a rotated/old cookie lingers (correct outcome, untidy). [app/web/routes_access.py:46]
- **Un-normalized exemption-prefix matching** — `is_exempt` does case-sensitive `startswith("/static/")`; case-variants, `//static`, and `/static/..` aren't normalized. Benign today (case-sensitive routes; `StaticFiles` rejects traversal) but fragile behind a path-rewriting proxy. [app/web/deps.py:44-48]
- **Unbounded form body on `POST /access`** — the one unauthenticated body-accepting endpoint has no token length / body-size cap (minor DoS surface). [app/web/routes_access.py:41]
- **No `?next=` deep-link preservation** — unauthenticated deep-link → redirected to `/access` → lands on `/` after entry, losing the intended destination. Latent until a third gated route exists. [app/main.py]
- **Forced-colors error icon** — State-2 denial icon is a USWDS sprite `background-image` that can drop in forced-colors mode; the word "Access token required." still carries the meaning (a11y floor met). Low-risk hardening only. [templates/access.html]
